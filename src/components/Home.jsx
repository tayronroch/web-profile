import React from 'react';

const Home = () => {
  return (
    <section
      id="home"
      className="h-screen w-full flex items-center justify-center transition-colors"
    >
      <h1 className="text-4xl font-bold">Bem-vindo(a) à Home!</h1>
    </section>
  );
};

export default Home;
